# Client

The connection layer: the gateway client, the standalone REST handle, and the
pieces they're built from.

```@autodocs
Modules = [Nobelium]
Pages = ["client.jl", "rest/api.jl", "rest/routes.jl", "rest/ratelimit.jl",
         "snowflake.jl", "permissions.jl", "intents.jl", "errors.jl", "formatting.jl",
         "cache.jl", "json.jl"]
```
