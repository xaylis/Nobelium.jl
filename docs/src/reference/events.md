# Events

Everything the gateway can hand your `on!` handlers.

```@autodocs
Modules = [Nobelium]
Pages = ["gateway/dispatch.jl", "gateway/events.jl", "gateway/events_base.jl",
         "gateway/shard.jl"]
```
